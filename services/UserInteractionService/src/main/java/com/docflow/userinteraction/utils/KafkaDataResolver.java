//TODO add resolver
//it will accept all kafka messages each in its own thread
//it will store all users who wait for a message in hash map by key
//all users will have associated key when requesting a message
